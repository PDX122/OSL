use std::collections::HashMap;
use std::sync::{Arc, Mutex};
use std::net::{TcpListener, TcpStream, SocketAddr};
use std::io::{Read, Write, BufReader, BufWriter};
use std::thread;
use std::time::Duration;
use actix_web::{App, HttpServer, web};

//! OSL Compiled Server


fn main() {
    ;
    ;
    ;
    ;
    ;
    let y: i64 = 2;
    let sum: i64 = (x + y);
    fn foo(a: i64) -> () {
    }
    
    ;
    ;
    return (a + 1);
    // Server: test
    let port = 8080;
    let host = "0.0.0.0";
    let workers = 4;
    // HTTP Configuration
    // HTTP config handled at runtime
    // Route: GET /
    // GET /
    // Route: GET /health
    // GET /health
    // Log Configuration
}
