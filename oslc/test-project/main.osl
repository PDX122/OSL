server "main" {
    port 8080;
    host "0.0.0.0";
    workers 4;
}

http {
    version HTTP1_1;
    compression gzip;
}

route GET "/" {
    respond 200 "Hello from OSL!";
}

route GET "/health" {
    respond 200 { "status": "ok" };
}

log {
    level info;
    format text;
    output [stdout];
}